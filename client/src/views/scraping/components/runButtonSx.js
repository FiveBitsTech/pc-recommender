// LoadingButton se deshabilita al cargar; el tema atenúa disabled (opacity 0.45) y en dark se pierde el spinner.
const runButtonSx = {
  '&.MuiLoadingButton-loading': {
    opacity: 1
  }
}

export default runButtonSx
